(executable
  (name "rabbitm")
  (public_name "rabbitm")
  (flags -w +a-4-27-29-50)
  (libraries sedlex menhirLib xml-light)
  (preprocess (pps sedlex.ppx)))

(menhir
  (modules parser)
  (flags --explain)
)

