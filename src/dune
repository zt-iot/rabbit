(executable
  (name "rabbit")
  (public_name "rabbit")
  (flags -w +a-4-70 -warn-error +a-4-70)
  (libraries sedlex menhirLib str)
  (preprocess (pps sedlex.ppx)))

(menhir
  (modules parser)
  (flags --explain --dump)
)

