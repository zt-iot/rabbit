(* Known gap: Rabbit-to-ProVerif compilation succeeds, but ProVerif rejects an
   equality as a standalone query premise. *)

const fresh value

type proc_t : process

process worker() : proc_t
{
  main { skip }
}

system worker()
requires
[
  lemma ReflexiveEquality : reachable value = value
]
