tag global [Valid:0]
fact global [False:0]

type person_t : process

process alice() : person_t 
{
    main {
    	event[::Valid()];

        repeat
        | [] →
	    skip
        until				        
        | [::False()] →  skip		        (* never terminate *)
        end
    }
}

system
  alice()
requires
[
  lemma Valid: (* verified *)
    reachable ::Valid()
]
