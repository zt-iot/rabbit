=== INPUT ===
process
  let x:bitstring suchthat x = a in
    let y = b in
      out(c, y)
  else
    out(c, e).
=== PARSED ===
process
  (let x:bitstring suchthat ((x) = (a)) in
     (let y = (b) in (out((c), (y))) else (out((c), (e))))).
=== EXPECTED ===
process
  let x:bitstring suchthat x = a in let y = b in out(c, y) else out(c, e).
