=== INPUT ===
process
  get storage(x:bitstring, y:bitstring) suchthat x = y in
    let z = b in
      out(c, z)
  else
    out(c, e).
=== PARSED ===
process
  (get storage(x:bitstring, y:bitstring) suchthat ((x) = (y)) in
   (let z = (b) in (out((c), (z))) else (out((c), (e))))).
=== EXPECTED ===
process
  get storage(x:bitstring, y:bitstring) suchthat x = y in
  let z = b in out(c, z) else out(c, e).
