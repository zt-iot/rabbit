fun hash/1.

free c.
private free x,n.

noninterf x among (n, hash(n)).

process
	out(c, x)

(* EXPECTPV
RESULT Non-interference x among (n[], hash(n[])) is true.
0.004s (user 0.004s + system 0.000s), max rss 6844K
END *)
